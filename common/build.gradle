plugins {
    id "immediatelyfast.base-conventions"
}

loom {
    accessWidenerPath = file("src/main/resources/immediatelyfast.accesswidener")
}

dependencies {
    modImplementation "net.fabricmc:fabric-loader:${project.fabric_loader_version}" // Needed to pull in mixin and mixin-extras
    api "net.lenni0451:Reflect:${project.reflect_version}"
}
